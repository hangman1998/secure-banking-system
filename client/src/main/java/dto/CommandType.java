package dto;

public enum CommandType {
    SIGN_UP,
    LOGIN,
    CREATE_ACCOUNT,
    REQUEST_JOIN,
    ACCEPT_JOIN,
    SHOW_ACCOUNT,
    DEPOSIT,
    WITHDRAW,
    TERMINATE,
    LOGOUT,
    TRANSFER
}
